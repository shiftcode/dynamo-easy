/**
 * @see https://prettier.io/docs/configuration
 * @type { import('prettier').Config }
 */
const config = {
  printWidth: 120,
  singleQuote: true,
  semi: false,
  trailingComma: "all",
};

export default config;
